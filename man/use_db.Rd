% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{use_db}
\alias{use_db}
\title{Temporarily use a specific database}
\usage{
use_db(path, db = NULL)
}
\arguments{
\item{path}{Character. Full path to a \code{.den} or \code{.sqlite} file, or a directory
path when \code{db} is also provided.}

\item{db}{Character. Filename, if \code{path} is a directory.}
}
\value{
Invisibly returns the resolved database path.
}
\description{
Sets \code{options(gopheR.db_path, gopheR.db_file)} to override den auto-detection.
Useful for pointing at a test database or a database outside the active den.
Call \code{\link[=use_den]{use_den()}} to clear the override and return to den auto-detection.
}
\examples{
\dontrun{
use_db("~/projects/test_project/test.den")
use_db("~/projects/test_project", "test.den")
use_den()  # revert to den auto-detection
}
}
