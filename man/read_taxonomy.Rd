% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amplicon.R
\name{read_taxonomy}
\alias{read_taxonomy}
\title{Ingest taxonomy assignments into a gopheR den}
\usage{
read_taxonomy(
  taxonomy_table,
  workflow_id,
  asv_batch_id = NULL,
  validate_only = FALSE,
  db_path = NULL
)
}
\arguments{
\item{taxonomy_table}{Path to a tab-delimited file or a data.frame. Column 1
is the ASV identifier (asv_id or local label); column 2 is the taxonomy
string. Additional columns (e.g. confidence) are ignored.}

\item{workflow_id}{Workflow ID for this classification run. Must already
exist in the \code{workflow} table.}

\item{asv_batch_id}{If provided, column 1 contains local ASV labels scoped
to this batch; they are mapped to global asv_ids via \code{amplicon_asv}. If
\code{NULL}, column 1 must already be asv_ids (MD5 hashes).}

\item{validate_only}{Logical. If \code{TRUE}, validate inputs without writing.}

\item{db_path}{Path to the \code{.den} database. Defaults to the active database.}
}
\value{
Invisibly returns a list with \code{n_inserted}.
}
\description{
Reads a two-column table of ASV identifiers and taxonomy strings and writes
them to \code{asv_taxonomy}. Each \verb{(asv_id, workflow_id)} pair is the primary
key, so re-running with the same workflow replaces prior assignments.
}
