% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amplicon.R
\name{read_amplicon}
\alias{read_amplicon}
\title{Ingest amplicon ASV count data into a gopheR den}
\usage{
read_amplicon(
  count_table,
  asv_batch_id,
  workflow_id,
  fasta_path = NULL,
  sample_map = NULL,
  validate_only = FALSE,
  db_path = NULL
)
}
\arguments{
\item{count_table}{Path to a tab-delimited file or a data.frame/tibble.
Column 1 must be local ASV labels. Remaining columns are sample names
with read counts. If \code{fasta_path} is \code{NULL}, column 2 must contain DNA
sequences (they are used for \code{asv_id} computation and then dropped from
the count data).}

\item{asv_batch_id}{Object ID of the existing \code{asv_batch} object for this
round of ASV generation. The object's subtype is used as \code{primer_set_id}.}

\item{workflow_id}{Workflow ID of the DADA2 workflow that produced this data.
Must already exist in the \code{workflow} table.}

\item{fasta_path}{Optional path to a FASTA file. If provided, sequences are
read from the FASTA (matched by ASV label) rather than from column 2 of
\code{count_table}.}

\item{sample_map}{Optional named character vector mapping local sample column
names to readset object IDs in the database. Names are local column names;
values are object IDs. If \code{NULL}, column names are assumed to already be
readset object IDs.}

\item{validate_only}{Logical. If \code{TRUE}, validates inputs without writing to
the database (no backup created).}

\item{db_path}{Path to the \code{.den} database. Defaults to the active database.}
}
\value{
Invisibly returns a list: \code{n_new} (ASVs inserted), \code{n_existing}
(ASVs already in \code{asv} table), \code{n_labels} (rows written to \code{amplicon_asv}).
}
\description{
Reads a post-DADA2 count table (local ASV labels x samples), deduplicates
sequences into the global \code{asv} table via MD5 hashing, records the per-batch
label -> global ID mapping in \code{amplicon_asv}, and registers the count table
file in \code{workflow_file}.
}
