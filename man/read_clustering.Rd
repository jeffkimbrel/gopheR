% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amplicon.R
\name{read_clustering}
\alias{read_clustering}
\title{Ingest ASV clustering results into a gopheR den}
\usage{
read_clustering(
  clustering_output,
  workflow_id,
  cluster_type,
  primer_set_id,
  format = c("uc", "tsv"),
  amplicon_id = NULL,
  trim = TRUE,
  validate_only = FALSE,
  db_path = NULL
)
}
\arguments{
\item{clustering_output}{Path to the clustering output file.}

\item{workflow_id}{Workflow ID for this clustering run. Must already exist.}

\item{cluster_type}{Short label for the clustering threshold, e.g.
\code{"cluster97"} or \code{"cluster99"}.}

\item{primer_set_id}{Primer set this clustering applies to (e.g. \code{"V4"}).
Used to scope \code{trim_clustering()}.}

\item{format}{\code{"uc"} (VSEARCH UC output, default) or \code{"tsv"} (two-column:
asv_id, cluster_id -- no representative information).}

\item{amplicon_id}{If provided, IDs in the file are local ASV labels from
this amplicon object; they are mapped to asv_ids via \code{amplicon_asv}.}

\item{trim}{Logical. If \code{TRUE} (default), call \code{trim_clustering()} after
inserting to archive older runs for this primer set + cluster type.}

\item{validate_only}{Logical.}

\item{db_path}{Path to the \code{.den} database.}
}
\value{
Invisibly returns a list with \code{n_rows} and \code{n_reps}.
}
\description{
Reads VSEARCH UC format or a simple two-column TSV and writes assignments to
\code{asv_cluster}. By default, immediately archives older clustering runs for the
same primer set and cluster type via \code{trim_clustering()}.
}
